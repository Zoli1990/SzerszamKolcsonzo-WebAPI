<Window x:Class="WpfSzerszamKolcsonzo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WpfSzerszamKolcsonzo.ViewModels"
        Title="Szerszámkölcsönző – Admin"
        Width="1280"
        Height="800"
        MinWidth="1024"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Background="#F5F1E8">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>

        <SolidColorBrush x:Key="TextPrimary" Color="#3D2F1F"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B5D4F"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E8DCC8"/>
        <SolidColorBrush x:Key="Warning" Color="#FF9800"/>
        <SolidColorBrush x:Key="Success" Color="#4CAF50"/>
        <SolidColorBrush x:Key="Danger" Color="#F44336"/>
        <SolidColorBrush x:Key="Primary" Color="#2196F3"/>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="PanelBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusBadge" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="1">
                    <Setter Property="Text" Value="AKTÍV"/>
                    <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="2">
                    <Setter Property="Text" Value="KIADVA"/>
                    <Setter Property="Foreground" Value="{StaticResource Primary}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.ColumnSpan="2" Background="#80000000" 
              Visibility="{Binding Loading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="999">
            <Border Background="White" CornerRadius="8" Padding="32"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏳" FontSize="24" Margin="0,0,12,0"/>
                    <TextBlock Text="Betöltés..." FontSize="18" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Column="0" Style="{StaticResource PanelBorder}" Margin="16" Padding="16">
            <StackPanel>
                <TextBlock Text="ÉRTESÍTÉSEK" Style="{StaticResource SectionTitle}"/>

                <Border BorderBrush="{StaticResource Warning}" BorderThickness="3" CornerRadius="6"
                        Padding="16" Background="#FFF9F0"
                        Visibility="{Binding HasPendingFoglalas, Converter={StaticResource BoolToVis}}">
                    <StackPanel DataContext="{Binding LatestPendingFoglalas}">
                        <TextBlock Text="🔔 ÚJ FOGLALÁS" FontWeight="Bold" FontSize="16"
                                   Foreground="{StaticResource Warning}" Margin="0,0,0,12"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Eszköz:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                       Text="{Binding Eszkoz.Nev}" Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Ügyfél:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" 
                                       Text="{Binding Felhasznalo.Nev, TargetNullValue='Ismeretlen'}" Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Kezdés:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" 
                                       Text="{Binding KezdetDatum, StringFormat='{}{0:yyyy.MM.dd. HH:mm}', TargetNullValue='Nincs'}" Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Ár:" 
                                       FontWeight="SemiBold" Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" 
                                       Text="{Binding Eszkoz.Ar, StringFormat='{}{0:N0} Ft'}" Margin="0,0,0,4"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Style="{StaticResource ActionButton}" Background="{StaticResource Success}"
                                    Content="✓ KIADVA"
                                    Command="{Binding DataContext.KiadvaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                            <Button Style="{StaticResource ActionButton}" Background="{StaticResource Danger}"
                                    Content="✗ NEM JÖTT" Margin="0"
                                    Command="{Binding DataContext.NemJottCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border BorderBrush="{StaticResource BorderLight}" BorderThickness="2" CornerRadius="6"
                        Padding="16" Background="#F9F9F9">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPendingFoglalas}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="✓" FontSize="48" Foreground="{StaticResource Success}"
                                   HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Nincs új foglalás" FontSize="14"
                                   Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Button Content="🔄 FRISSÍTÉS" Style="{StaticResource ActionButton}"
                        Background="{StaticResource Primary}" Command="{Binding RefreshCommand}"
                        Margin="0,16,0,0" HorizontalAlignment="Stretch"/>

                <Button Content="🧪 Teszt Értesítés" Style="{StaticResource ActionButton}"
                        Background="#9C27B0" Command="{Binding TestNotificationCommand}"
                        Margin="0,8,0,0" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource PanelBorder}" Margin="16" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="AKTÍV FOGLALÁSOK" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="{Binding AktivFoglalasok.Count, StringFormat='({0} db)'}"
                               FontSize="16" Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center" Margin="8,0,0,12"/>
                </StackPanel>

                <ListView Grid.Row="1" ItemsSource="{Binding AktivFoglalasok}"
                          Margin="0,8,0,0" BorderThickness="0" Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="8"/>
                            <Setter Property="Margin" Value="0,0,0,4"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Státusz" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource StatusBadge}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Eszköz" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Eszkoz.Nev}" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Ügyfél" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Felhasznalo.Nev, TargetNullValue='Ismeretlen'}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Kiadás" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding KiadasDatum, StringFormat='{}{0:MM.dd. HH:mm}', TargetNullValue='-'}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Díj" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Eszkoz.Ar, StringFormat='{}{0:N0} Ft'}"
                                                   FontWeight="SemiBold" Foreground="{StaticResource Success}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Műveletek" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="↩ Vissza" Height="32" Padding="12,0"
                                                Background="{StaticResource Success}" Foreground="White"
                                                BorderThickness="0" Cursor="Hand"
                                                Command="{Binding DataContext.VisszahozvaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="2">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Window>
